var app = {};

app.width = 500;
app.height = 300;

var r = new Rune({
  container: "#display",
  // framerate: 90,
  // debug: true,
  width: app.width,  // Alternative: "100%"
  height: app.height // Alternative: "100%"
});
